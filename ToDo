* Build 0.3.7 setup for Windows (Remove the old settings file)
* Build 0.3.7 portable for Windows

* New layout for MainFrame
* Redesign options Window
* Add context menu (issue #16)
* Use import instead of subprocess
